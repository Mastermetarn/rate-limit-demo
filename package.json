{
  "name": "rate-limit-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal interactive token bucket rate limiting demo",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.1.0"
  }
}
